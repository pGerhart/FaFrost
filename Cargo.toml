[package]
name = "fafrost"
version = "0.1.0"
edition = "2024"

[features]
bip340 = []

[dependencies]
bitcoin = "0.32"
rand = "0.8"
rand_core = "0.6"
sha2 = "0.10"
k256 = { version = "0.13", default-features = false, features = [
    "arithmetic",
    "hash2curve",
] }
